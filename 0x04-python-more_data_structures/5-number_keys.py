#!/usr/bin/python
def number_keys(a_dictionary):
    """extract/get all the keys in the dic"""
    num_keys = a_dictionary.keys()
    """using len() function to return total number of keys"""
    return(len(num_keys))
