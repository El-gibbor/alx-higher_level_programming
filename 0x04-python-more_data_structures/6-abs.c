#include "main.h"

/**
 * _abs - computes the absolute value of an int.
 * @x: Paremeter to compute
 * Return: Always 0 (Success 0)
 */

int _abs(int x)
{
	if (x >= 0)
		return (x);
	else
		return (-x);
}
